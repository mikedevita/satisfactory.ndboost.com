import { Layout, AddEdit } from 'components/gameservers';

export default Add;

function Add() {
    return (
        <Layout>
            <h1>Add Game Server</h1>
            <AddEdit />
        </Layout>
    );
}